import 'package:flutter/material.dart';

class OrdersTabletView
 extends StatelessWidget {
  const OrdersTabletView
  ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Orders Tablet View'),
      ),
    );
  }
}