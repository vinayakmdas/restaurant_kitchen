import 'package:flutter/material.dart';

class OrdersMobileView extends StatelessWidget {
  const OrdersMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Orders Mobile View'),
      ),
    );
  }
}